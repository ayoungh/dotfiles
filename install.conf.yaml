- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig

- create:
    - ~/Dev

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]