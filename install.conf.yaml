- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.hyper.js: .hyper.js
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig

- create:
    - ~/Developer

- shell:
    -
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true