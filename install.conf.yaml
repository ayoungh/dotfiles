- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig

- create:
    - ~/Dev

- shell:
    -
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true